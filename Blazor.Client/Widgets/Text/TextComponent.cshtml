@Text <sub>(powered by widget)</sub>

@functions
{
    [Parameter] protected string Text { get; set; }

    public void SetText(string text)
    {
        Text = text;
        StateHasChanged();
    }
}